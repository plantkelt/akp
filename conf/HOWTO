
1. Create POSTGRESQL database
$ su postgres
$ createuser akp
$ createdb -E UTF8 -O akp akp

2. Configure APACHE
$ cp akp /etc/apache2/sites-available/
$ ln -s ../sites-available/akp /etc/apache2/sites-enabled/001-akp
$ /etc/init.d/apache restart

3. Configure TOMCAT
$ mkdir /opt/akp
$ cp akpwebapp.war /opt/akp/
$ cp log4j.properties /opt/akp/
$ cp akp.xml /etc/tomcat/Catalina/localhost/

